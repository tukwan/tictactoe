{
  "name": "tictactoe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon src/app.ts",
    "start": "PORT=10000 node dist/app.js",
    "build": "tsc",
    "build:interface": "cd ./interface && npm install && npm run build",
    "build:prod": "npm install && npm run build && npm run build:interface"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "~4.16.1",
    "socket.io": "^4.7.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.5",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
